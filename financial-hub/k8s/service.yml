apiVersion: v1
kind: Service
metadata:
  name: financial-hub-service
  labels:
    app: financial-hub
spec:
  type: LoadBalancer
  selector:
    app: financial-hub
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
