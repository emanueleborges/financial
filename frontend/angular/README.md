# Financial Hub — Angular (UI da vaga)

Spec: [`specs/frontend/angular.md`](../../specs/frontend/angular.md)

```bash
cd frontend/angular
npm install
npm start   # http://localhost:4200
```

API em `http://localhost:8080`. Inbox de notificações em `http://localhost:8081`.
